# from prometheus_api_client.utils import parse_datetime, parse_timedelta
# from prometheus_api_client import Metric, MetricsList
# # Start time is 3 days before the current timestamp.
# start_time = parse_datetime("60m")
# end_time = parse_datetime("now")  # End time is now.
# chunk_size = parse_timedelta("now", "1m")  # Chunk size is 1 day.

# # `scrape_duration_seconds` is the time taken by prometheus to scrape it's targets for metrics.
# metric_name = "istio_request_duration_milliseconds_bucket"

# metric_data = prom_api.get_metric_range_data(
#     metric_name,
#     start_time=start_time,
#     end_time=end_time,
#     chunk_size=chunk_size,
# )
# metrics_object_list = MetricsList(metric_data)
# print(len(metrics_object_list))
# for item in metrics_object_list:
#     print(item.metric_name, item.label_config, "\n")
